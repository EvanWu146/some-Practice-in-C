#include<stdio.h>
#include<stdlib.h>
typedef struct listnode{
	int data;
	struct listnode *nextPtr;
}NODE; 
typedef NODE *NODEPTR;
NODEPTR createnode(){
	NODEPTR currentptr=NULL,lastptr=NULL,headptr=NULL;
	int num;
	headptr=(NODEPTR)malloc(sizeof(NODE));
	headptr->nextPtr
	if(headptr!=NULL){
		scanf("%d",&num);
		while(num!=-1){
			currentptr=(NODEPTR)malloc(sizeof(NODE));
			if(currentptr!=NULL){
				current
			}
		}
	}
}
